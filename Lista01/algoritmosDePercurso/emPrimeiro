emPrimeiro(raiz) {
    if(raiz == null){
        return null;
    }

    u = raiz;
    while(u.esq != null){
        u = u.esq;
    }

    return u;
}

1. checar se não samos nulo
2. vamos descer a esquerda até o ultimo nó a esquerda
3. vamos retorna esse ultimo nó a esrqueda ou a propria raiz